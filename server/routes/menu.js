// import express from "express";


// const router = express.Router();

// router.get("/menu", async (req, res) => {
//   try {
//     const menu = await knex("dinner_menu").select("*");
//     res.json(menu);
//   } catch (error) {
//     res.status(500).json({ error: "Failed to fetch menu" });
//   }
// });

// export default router;
